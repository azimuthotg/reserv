# Progress 2026-08-10 — เตือนเมื่อ "ข้อมูลวันหยุดค้าง" (Claude Code edit)

## สิ่งที่ทำ

ปิดช่องโหว่เชิงออกแบบของแถบเตือนวันหยุดที่ทำไว้เมื่อ 2026-08-09

**ปัญหา:** แถบเตือนบนแดชบอร์ดคิวรี `HolidayDate.filter(source=auto, is_active=False, date ใน 30 วัน)`
คือขึ้นได้เฉพาะเมื่อ **มีแถวฉบับร่างอยู่จริงในฐาน** แถวพวกนี้เกิดจากการกดปุ่ม sync เท่านั้น
(ผู้ใช้เลือกไม่ตั้ง Task Scheduler) ทำให้แดชบอร์ดที่เงียบเพราะ *ตรวจครบแล้ว* แยกไม่ออกจากที่เงียบเพราะ
*ไม่มีใครกดดึงมาหลายเดือน* — รูปแบบเดียวกับตอนตารางค้างตั้งแต่ 3 มิ.ย. จนมีคนจอง 12 ส.ค. ได้ 2 รายการ

**อันตรายจริง** ไม่ใช่ขอบฟ้าข้อมูลหมด (มีถึง 28 ก.ค. 2570) แต่คือวันหยุดพิเศษที่ ครม. ประกาศเพิ่มกลางปี
ซึ่งจะไม่เข้าระบบเลยถ้าไม่มีใครกดดึงซ้ำ

**ผู้ใช้เลือกทางแก้ B อย่างเดียว** (ไม่เอา Task Scheduler — "ไม่อยากสร้างภาระให้ prod")

## ไฟล์ที่แก้

| ไฟล์ | สิ่งที่ทำ |
|---|---|
| `booking/models.py` | เพิ่ม `HolidaySyncRun` + `data_status()` + ค่าคงที่ `HOLIDAY_SYNC_STALE_DAYS=45`, `HOLIDAY_HORIZON_MIN_DAYS=60` |
| `booking/migrations/0017_holidaysyncrun.py` | สร้างตาราง + **data migration bootstrap** แถวแรกถ้ามี `source='auto'` อยู่แล้ว |
| `booking/manage_views.py` | บันทึก `HolidaySyncRun` หลัง sync ผ่านปุ่ม (แม้ได้ 0 วัน) · ส่ง `holiday_sync` เข้าทั้งแดชบอร์ดและหน้าจัดการวันหยุด |
| `booking/management/commands/sync_holidays.py` | บันทึก `HolidaySyncRun` เมื่อรันจริง (ไม่บันทึกตอน `--dry-run` / fetch ล้ม) |
| `booking/templates/booking/manage/dashboard.html` | แถบเหลืองใหม่ — แยกจากแถบแดง "ฉบับร่างค้าง" เดิม |
| `booking/templates/booking/manage/holidays.html` | บรรทัดสถานะ "ดึงล่าสุดเมื่อ / ข้อมูลมีถึง" ติดปุ่มดึง |
| `booking/admin.py` | ขึ้น Django Admin แบบอ่านอย่างเดียว (ปิด add/change) |
| `booking/tests.py` | คลาสใหม่ `HolidayFreshnessTests` 14 เคส |
| `CLAUDE.md`, `AGENTS.md` | sync หัวข้อ "เตือนข้อมูลวันหยุดค้าง" + ตาราง Models |
| `MEM.md` | บันทึกการตัดสินใจ + กับดัก 4 ข้อ |

## เกณฑ์การเตือน

แถบขึ้นเมื่อ `needs_attention` = `is_stale or horizon_short`

- `is_stale` — ไม่ได้ดึงเกิน 45 วัน หรือไม่เคยดึงเลย
- `horizon_short` — วันหยุดล่าสุดในตารางอยู่ใกล้กว่า 60 วัน หรือไม่มีวันหยุดเลย

## กับดักที่แก้ไว้แล้ว (อย่ารื้อ)

1. **บันทึกแม้ `created_count=0`** — ถ้าใช้ "วันที่สร้างล่าสุดของ `HolidayDate`" แทน sync ที่รันตรงเวลา
   แต่ไม่เจอวันใหม่จะดูเหมือนไม่เคยรัน
2. **fetch ล้มเหลว / `--dry-run` ต้องไม่บันทึก** — ไม่งั้นความล้มเหลวถูกนับเป็น "ดึงแล้ว"
3. **data migration bootstrap** — ไม่งั้นวัน deploy แดชบอร์ดขึ้น "ยังไม่เคยดึงเลย" ทั้งที่เพิ่งดึงจริงเมื่อวาน
   สัญญาณเตือนผิดตัวตั้งแต่วันแรกทำให้เจ้าหน้าที่เลิกเชื่อแถบเตือน
4. **`timezone.localtime()` ก่อนคิดวัน** — `synced_at` เก็บ UTC อ่านตรง ๆ วันเพี้ยนได้ 1 วัน

## การตรวจสอบ

```
python manage.py check                          → 0 issues
python manage.py makemigrations booking --check → No changes detected
python manage.py test booking                   → Ran 70 tests OK (เดิม 56 · เพิ่มใหม่ 14)
```

## งานค้าง

- **ยังไม่ deploy ขึ้น prod** — มี migration `0017` ต้องรัน `migrate` ด้วย:
  `git pull` → `.\venv\Scripts\python.exe manage.py migrate` → `collectstatic` → `nssm restart Reserv`
- ระบบยังพึ่งคนกดปุ่มดึงอยู่ แถบเตือนบอกได้ว่า "ถึงเวลาแล้ว" แต่ไม่ได้ดึงให้เอง
  (Task Scheduler ผู้ใช้ปฏิเสธแล้ว — คำสั่งยังอยู่ใน `doc/deploy_guide.md` ถ้าเปลี่ยนใจ)
