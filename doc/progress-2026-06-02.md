# ความก้าวหน้า — 2 มิถุนายน 2569

<!-- Codex edit: documentation synchronization after static code review. -->

## ผู้ดำเนินการ

Codex

## เป้าหมาย

ตรวจ flow การ login และการเข้าใช้งานจาก implementation จริง เพื่อเตรียมข้อมูลสำหรับป้ายประชาสัมพันธ์ก่อนเปิดภาคเรียน
จากนั้น sync เอกสารกำกับงานสำหรับ Codex และ Claude Code ให้ใช้ข้อมูลล่าสุดร่วมกัน

## ผลการตรวจ flow ปัจจุบัน

ผู้ใช้ทั่วไปเข้าใช้งานได้ 2 ช่องทาง:

| ช่องทาง | Flow ปัจจุบัน |
|---|---|
| LINE OA | กด Rich Menu → เปิด LIFF → LINE Login → ตรวจการผูกบัญชี → จอง |
| เว็บไซต์ | เปิด `https://lib.npu.ac.th/reserv/` → LINE Login → ตรวจการผูกบัญชี → จอง |

ทั้งสองช่องทางใช้ LINE LIFF เป็นตัวตนหลัก ไม่มี session login แยกสำหรับผู้ใช้ทั่วไปบนเว็บไซต์
Django session login ใช้เฉพาะ Staff Portal (`/manage/`)

การเข้าใช้งานครั้งแรก:
1. ระบบรับ LINE userId จาก `liff.getProfile()`
2. ระบบเรียก `POST /api/check-user/`
3. หากยังไม่ผูกบัญชี ระบบพาไป `/register/`
4. ผู้ใช้เลือกประเภทผู้ใช้ และกรอก LDAP/password
5. ระบบตรวจ LDAP และผูก LINE userId กับบัญชีมหาวิทยาลัย
6. ครั้งถัดไปเข้าใช้งานได้ทันทีโดยไม่ต้องกรอก LDAP ซ้ำ

ประเภทผู้ใช้:
- นักศึกษาเลือก `นักศึกษา`
- อาจารย์และบุคลากรเลือก `บุคลากรภายในมหาวิทยาลัย`

หน้าสาธารณะที่ไม่ต้อง login:
- `/calendar/`
- `/room/<booking_name>/`

## ไฟล์ที่แก้ไข

| ไฟล์ | การเปลี่ยนแปลง |
|---|---|
| `AGENTS.md` | อัปเดต architecture, URL, auth flow, NPU helper, model, settings, phase และ workflow การส่งต่องาน |
| `CLAUDE.md` | sync เนื้อหาหลักให้ตรงกับ `AGENTS.md` พร้อม comment ว่าเป็น Codex edit |
| `doc/poster-content.md` | แก้ข้อความเว็บไซต์ให้ตรงกับ LINE Login และอัปเดตเวลาเปิดบริการ |
| `doc/poster-content.txt` | sync เนื้อหาสำหรับนำไปใช้จัดทำสื่อ |
| `doc/poster-booking-guide.html` | แก้ต้นฉบับโปสเตอร์ HTML ให้ตรงกับ flow และเวลาเปิดบริการล่าสุด |
| `doc/progress-2026-06-02.md` | เพิ่ม progress log ฉบับนี้เพื่อใช้อ้างอิง |

## ข้อมูลเดิมที่แก้ไข

- ลบข้อมูล `/api/set-session/` เพราะไม่มี endpoint นี้ใน implementation ปัจจุบัน
- แก้ข้อความ “เว็บไซต์ใช้ session login” เป็น “เว็บไซต์ยังใช้ LINE Login”
- แก้ตำแหน่ง NPU helper จาก `booking/utils.py` เป็น `booking/views.py`
- แก้ IoT flow จาก `api.npu.ac.th/sonoff/` เป็น Django เรียก Home Assistant โดยตรง
- แก้ LIFF Endpoint URL สำหรับ LINE Developers Console เป็น `https://lib.npu.ac.th/reserv/`
- อัปเดต phase ตามฟีเจอร์ที่พบใน codebase และใส่หมายเหตุให้ตรวจ production deployment ก่อนประกาศใช้

## ข้อตกลงการทำงานร่วมกันต่อไป

ใช้ `AGENTS.md` และ `CLAUDE.md` เป็นเอกสารคู่สำหรับ Codex และ Claude Code โดยรักษาข้อมูลเชิงระบบให้ตรงกัน

ทุกครั้งที่ behavior, architecture, URL, auth flow, deployment หรือสถานะ feature เปลี่ยน:
1. sync เอกสารทั้งสองไฟล์
2. สร้างหรืออัปเดต `doc/progress-YYYY-MM-DD.md`
3. ระบุผู้แก้ เช่น `Codex edit` หรือ `Claude Code edit`
4. ระบุไฟล์ที่เกี่ยวข้อง วิธีตรวจสอบ และงานค้าง
5. ตรวจ diff ก่อนส่งต่องาน

## ยืนยันผลทดสอบบน production

ผู้ดูแลระบบยืนยันผลทดสอบแล้ว:

- ระบบยกเลิกการจองอัตโนมัติเมื่อไม่ Check-in ภายในเวลาที่กำหนดทำงานได้
- IoT ปิดอุปกรณ์ตามเวลาสิ้นสุดการจองได้

เวลาเปิดบริการสำหรับใช้จัดทำสื่อ:

| วัน | เวลา |
|---|---|
| จันทร์ – ศุกร์ | 08:30 – 16:30 น. |
| เสาร์ – อาทิตย์ | 09:00 – 17:00 น. |
| วันหยุดนักขัตฤกษ์ | ปิดให้บริการ โดยแจ้งล่วงหน้า |

## งานค้างที่ควรพิจารณาก่อนประชาสัมพันธ์วงกว้าง

พบจาก static review ยังไม่ได้แก้ code:

1. API สำหรับผู้ใช้หลายจุดรับ `userId` จาก browser โดยยังไม่ได้ตรวจ LIFF access token ฝั่ง server

แก้ไขแล้วในรอบนี้:

- แก้ข้อความต้นฉบับโปสเตอร์ให้ระบุว่าเว็บไซต์ยืนยันตัวตนด้วย LINE Login
- อัปเดตเวลาเปิดบริการสำหรับป้ายประชาสัมพันธ์
- เพิ่ม guard ตรวจ `LineUser.is_active` ใน `create_booking()`
- ผูก `sessionStorage` profile cache กับ LINE userId

## เวลาเปิดบริการในระบบจอง

สถานะ: ✅ แก้ใน Code Fix 5 แล้ว

ระบบเก็บ `Room.open_time` และ `Room.close_time` เป็นเวลาเปิด-ปิดวันจันทร์ – ศุกร์
ส่วนเสาร์ – อาทิตย์ใช้ policy กลาง `09:00 – 17:00 น.` จาก `booking/service_hours.py`

backend ตรวจซ้ำทุกครั้งก่อนสร้าง booking ว่าเวลาอยู่ภายในช่วงเปิดบริการของวันที่เลือก

## ลำดับการแก้ code สำหรับ production

เรียงจากผลกระทบน้อยไปมาก เพื่อ deploy และย้อนกลับได้เป็นรอบเล็ก:

| ลำดับ | งาน | ผลกระทบโดยประมาณ | แนวทาง deploy |
|---|---|---|---|
| 1 | ตรวจผล `_register_npu_user()` ก่อนสร้าง local user | ต่ำ | กระทบเฉพาะผู้ใช้ลงทะเบียนใหม่ ทดสอบ register สำเร็จและกรณี NPU API ล้มเหลว |
| 2 | ตรวจ `LineUser.is_active` ก่อนสร้าง booking | ต่ำ | เพิ่ม guard จุดเดียว ผู้ใช้ active เดิมไม่เปลี่ยน behavior |
| 3 | ผูก `sessionStorage` cache key กับ LINE userId | ต่ำ – กลาง | แตะ frontend หน้า landing, booking และ card ผู้ใช้เดิมอาจ login ใหม่หนึ่งครั้ง |
| 4 | บังคับ `LineUser.is_active` ให้ครบทุก API ที่ต้องใช้สิทธิ์ | กลาง | แตะ check-user, my-bookings, cancel, check-in และ IoT control ต้อง regression test flow หลัก |
| 5 | รองรับเวลาแยกวันธรรมดา/วันหยุดสุดสัปดาห์ และตรวจเวลาฝั่ง backend | กลาง | แตะ booking policy, UI slot และหน้า staff schedule ควร deploy แยก commit |
| 6 | ตรวจ LIFF access token ฝั่ง server แทนการเชื่อ `userId` จาก browser | สูง | แตะ contract หลาย API และ frontend หลายหน้า ต้องเตรียมค่า LINE channel ID และทดสอบผ่าน HTTPS บน production |

ทุกลำดับควรทำเป็น commit แยก แล้ว push เพื่อทดสอบ production ทีละรอบ หากพบปัญหาจะย้อนกลับได้ง่าย

## Code Fix 1 — Registration Guard

<!-- Codex edit: low-impact production fix, pending production verification. -->

สถานะ: ✅ ผู้ดูแลระบบทดสอบบน production แล้ว ทำงานถูกต้อง

แก้ `booking/views.py` ให้ตรวจผล `_register_npu_user()` หลัง LDAP ผ่าน:

- หาก NPU API ผูกบัญชีสำเร็จ จึงสร้างหรือ refresh `LineUser` ใน local DB และ redirect ต่อ
- หาก NPU API ผูกบัญชีไม่สำเร็จ ระบบคงอยู่หน้าลงทะเบียนและแสดงข้อความให้ลองใหม่
- ป้องกันข้อมูลค้างที่ local DB เมื่อ source of truth ผูกบัญชีไม่สำเร็จ

ผลกระทบ:
- กระทบเฉพาะผู้ใช้ที่กำลังลงทะเบียนผูกบัญชีใหม่
- ผู้ใช้ที่ผูกบัญชีแล้ว การจอง การยกเลิก Check-in และ IoT ไม่เปลี่ยน behavior

แผนทดสอบ production:
1. ลงทะเบียนผู้ใช้ใหม่ที่ข้อมูล LDAP ถูกต้อง ต้องผูกบัญชีและ redirect เข้าใช้งานได้
2. หาก NPU API ไม่พร้อมใช้งาน ต้องเห็นข้อความ `ไม่สามารถผูกบัญชีกับระบบมหาวิทยาลัยได้ กรุณาลองใหม่อีกครั้ง`
3. ตรวจว่าผู้ใช้เดิมยังเข้าใช้งานและจองได้ตามปกติ

การตรวจสอบก่อน push:
- `AST parse OK: booking/views.py`
- `git diff --check` ผ่าน ไม่พบ whitespace error
- `python manage.py check` ยังรันไม่ได้ใน environment local เพราะยังไม่มี package `python-dotenv` แม้ระบุอยู่ใน `requirements.txt`
- `python -m py_compile booking/views.py` ใช้ไม่ได้ใน sandbox เนื่องจากไม่มีสิทธิ์เขียน `booking/__pycache__` จึงใช้ AST parse แบบไม่เขียนไฟล์แทน

ผลทดสอบ production:
- ผู้ดูแลระบบยืนยันว่าการลงทะเบียนและเข้าใช้งานทำงานถูกต้อง
- พบการหน่วงเล็กน้อยช่วงตรวจสอบสิทธิ์ สันนิษฐานว่าเกี่ยวกับ network latency หรือ external NPU API
- Code Fix 1 ไม่ได้เพิ่มจำนวน external request ในกรณีปกติ เพียงตรวจผล request เดิมก่อนสร้าง local user

## Code Fix 2 — Block Inactive User Booking

<!-- Codex edit: low-impact production fix, pending production verification. -->

สถานะ: ✅ ผู้ดูแลระบบทดสอบบน production แล้ว ระงับผู้ใช้และปฏิเสธการสร้าง booking ได้ถูกต้อง

แก้ `create_booking()` ใน `booking/views.py` ให้ปฏิเสธการสร้าง booking เมื่อ `LineUser.is_active=False`

- ผู้ใช้ active เดิมยังจองได้ตามปกติ
- ผู้ใช้ที่เจ้าหน้าที่ปิดใช้งานจะได้รับข้อความ `บัญชีของคุณถูกระงับการใช้งาน กรุณาติดต่อเจ้าหน้าที่`
- ไม่เปลี่ยน behavior ของการยกเลิก Check-in หรือ IoT ในรอบนี้

แผนทดสอบ production:
1. ผู้ใช้ active ต้องสร้าง booking ได้ตามปกติ
2. ปิดใช้งานผู้ใช้จาก Staff Portal แล้วลองสร้าง booking ต้องถูกปฏิเสธ
3. เปิดใช้งานผู้ใช้อีกครั้ง แล้วต้องสร้าง booking ได้ตามปกติ

## Production Investigation — Code Fix 2

<!-- Codex edit: inspected production through authenticated Staff Portal and documented the deployment reload requirement. -->

สถานะ: ✅ ผู้ดูแลระบบทดสอบบน production แล้ว ระงับผู้ใช้และปฏิเสธการสร้าง booking ได้ถูกต้อง

หลักฐานจาก Staff Portal เมื่อวันที่ `02/06/2569`:

- ผู้ใช้ `นายสุรนารถ สุพรรณ` เป็น record `#4` และมีสถานะ `ปิดใช้`
- พบ booking `#78` ห้อง `Edutainment Zone` เวลา `11:50–12:00 น.` สถานะ `ยืนยันแล้ว`
- booking `#78` ถูกสร้างเมื่อ `02/06/2569 11:24:48`
- URL สำหรับสร้าง booking มีเส้นทางเดียวคือ `/api/booking/` ซึ่งเรียก `create_booking()`
- commit `105fd6a fix: block inactive users from creating bookings` มี guard ตรวจ `LineUser.is_active=False` ก่อนสร้าง booking แล้ว

สาเหตุของผลทดสอบรอบแรก: production ยังไม่ได้ pull commit `105fd6a` จึงยังทำงานด้วย code เก่า ไม่ใช่ defect เพิ่มเติมใน guard

ขั้นตอน deploy และทดสอบบน production:

1. pull commit ล่าสุด
2. รัน `nssm restart reserv-booking`
3. ลองสร้าง booking ด้วยผู้ใช้ที่ปิดใช้งาน ต้องได้รับข้อความให้ติดต่อเจ้าหน้าที่
4. เปิดใช้งานผู้ใช้อีกครั้ง แล้วทดสอบว่าสร้าง booking ได้ตามปกติ

ผลทดสอบหลัง pull code ล่าสุด:

- ปิดใช้งานผู้ใช้จาก Staff Portal ได้
- ผู้ใช้ที่ถูกปิดใช้งานไม่สามารถสร้าง booking ใหม่ได้
- Code Fix 2 ทำงานถูกต้องบน production

## Code Fix 3 — Bind Frontend Cache To LINE User

<!-- Codex edit: low-impact frontend cache isolation fix, verified on production. -->

สถานะ: ✅ ผู้ดูแลระบบทดสอบบน production แล้ว ทำงานถูกต้อง

แก้ profile cache ใน `sessionStorage` จาก key กลาง `npu_user_v1`
เป็น key รูปแบบ `npu_user_v2:<LINE userId>` ในหน้า:

- `booking/templates/booking/landing.html`
- `booking/templates/booking/booking.html`
- `booking/templates/booking/card.html`

แต่ละหน้าจะลบ legacy key `npu_user_v1` เมื่อโหลด profile และอ่านหรือเขียน cache เฉพาะ LINE userId ปัจจุบัน

ผลกระทบ:

- ป้องกัน profile cache ของบัญชี LINE เดิมถูกนำไปแสดงหรือใช้ต่อเมื่อสลับบัญชี
- ผู้ใช้เดิมจะ refresh profile จาก API หนึ่งครั้งหลัง deploy เพราะไม่ใช้ legacy key ต่อ
- ไม่เปลี่ยน backend API, database schema, booking logic, Check-in หรือ IoT

แผนทดสอบ production:

1. ผู้ใช้เดิมเปิดหน้าแรกและหน้าจอง ต้องใช้งานและสร้าง booking ได้ตามปกติ
2. เปิดบัตรสมาชิก ต้องแสดงข้อมูลและ QR Code ของ LINE user ปัจจุบัน
3. สลับบัญชี LINE แล้วเปิดระบบใหม่ ต้องไม่เห็นชื่อ คณะ หรือ QR Code ของบัญชีก่อนหน้า

ผลทดสอบ production:

- หน้าแรกและหน้าจองใช้งานได้ตามปกติ
- บัตรสมาชิกแสดงข้อมูลและ QR Code ถูกต้อง
- สลับบัญชี LINE แล้วไม่พบข้อมูล profile cache ค้างจากบัญชีก่อนหน้า
- Code Fix 3 ทำงานถูกต้องบน production

## Code Fix 4 — Enforce Inactive User Guard Across APIs

<!-- Codex edit: medium-impact permission guard hardening, verified on production. -->

สถานะ: ✅ ผู้ดูแลระบบทดสอบบน production แล้ว ทำงานถูกต้อง

เพิ่ม helper `_get_active_line_user()` ใน `booking/views.py` เพื่อให้ API ที่ต้องใช้สิทธิ์ผู้ใช้
ปฏิเสธ `LineUser.is_active=False` ด้วย HTTP `403` และข้อความ
`บัญชีของคุณถูกระงับการใช้งาน กรุณาติดต่อเจ้าหน้าที่`

API ที่เพิ่ม guard:

- `access_status()`
- `check_user()`
- `create_booking()`
- `my_bookings()`
- `cancel_booking()`
- `checkin_booking()`
- `walai_card()`
- `room_status()`
- `device_toggle()`

เพิ่ม `line_user__is_active=True` ใน `_get_active_booking()` เป็นชั้นป้องกันเสริมสำหรับ IoT
และจำกัด field ของ `LineUser.save()` ตอน refresh profile เพื่อไม่ให้เขียนทับสถานะปิดใช้จาก Staff Portal

หน้า landing, booking และ card จะแสดงข้อความติดต่อเจ้าหน้าที่เมื่อ API คืนค่า `blocked`
แทนการ redirect ไปหน้าลงทะเบียน และเรียก `/api/access-status/` จาก local DB
ก่อนอ่าน `sessionStorage` เพื่อให้การระงับมีผลทันทีแม้ browser มี profile cache เดิม

ผลกระทบ:

- ผู้ใช้ active เดิมต้องใช้งาน flow หลักได้ตามปกติ
- ผู้ใช้ที่ถูกระงับไม่สามารถดูรายการจอง ยกเลิก Check-in เปิดบัตรสมาชิก หรือติดต่อ IoT API ได้
- ไม่เปลี่ยน database schema, public calendar หรือข้อมูลตารางห้อง

แผนทดสอบ production:

1. ผู้ใช้ active เปิดหน้าแรก ดูรายการจอง สร้าง booking และยกเลิก booking ได้ตามปกติ
2. ผู้ใช้ active Check-in และควบคุม IoT ได้ตามช่วงเวลาที่มีสิทธิ์
3. ปิดใช้งานผู้ใช้จาก Staff Portal แล้วเปิดระบบใหม่ ต้องเห็นข้อความให้ติดต่อเจ้าหน้าที่
4. ผู้ใช้ที่ถูกปิดใช้งานต้องสร้าง booking, ดูรายการจอง, ยกเลิก, Check-in และเรียก IoT ไม่ได้
5. เปิดใช้งานผู้ใช้อีกครั้ง แล้ว flow หลักต้องกลับมาใช้งานได้ตามปกติ

การตรวจสอบก่อน push:

- `AST parse OK: booking/views.py, booking/urls.py`
- in-memory SQLite regression test ผ่าน: inactive user ได้ HTTP `403` และ `blocked=True`
  จาก `access-status`, `check-user`, `create-booking`, `cancel-booking`, `my-bookings`,
  `checkin`, `walai-card`, `room-status` และ `device-toggle`
- in-memory SQLite regression test ผ่าน: active user ได้ `active=True` และผู้ใช้ที่ยังไม่มี local record ได้ `registered=False`
- frontend regression check ผ่าน: landing, booking และ card เรียก `/api/access-status/` ก่อนอ่าน `sessionStorage`
- URL route static check ผ่าน: `/api/access-status/`
- `git diff --check` ผ่าน ไม่พบ whitespace error
- `python manage.py check` ยังรันไม่ได้ใน environment local เพราะไม่มี package `python-dotenv`
  แม้ระบุอยู่ใน `requirements.txt`

ผลทดสอบ production:

- ผู้ดูแลระบบ pull code และทดสอบ flow หลักแล้ว
- การระงับสิทธิ์ผู้ใช้ทำงานถูกต้องบน production
- Code Fix 4 ทำงานถูกต้องบน production

## Code Fix 5 — Weekend Service Hours And Backend Validation

<!-- Codex edit: medium-impact booking policy update, pending production verification. -->

สถานะ: ดำเนินการแล้ว รอผู้ดูแลระบบ pull และทดสอบบน production

เพิ่ม `booking/service_hours.py` เป็น policy กลาง:

- จันทร์ – ศุกร์: อ่านเวลาจาก `Room.open_time` และ `Room.close_time` เดิม
- เสาร์ – อาทิตย์: ใช้ `09:00 – 17:00 น.`

แก้ `create_booking()` ให้ปฏิเสธเวลาเริ่มหรือสิ้นสุดที่อยู่นอกช่วงเปิดบริการของวันที่เลือก
และปรับส่วนแสดงผล:

- หน้าแรกแสดงเวลาวันธรรมดาและวันหยุดสุดสัปดาห์
- ฟอร์มจองเปลี่ยน dropdown และ quick slots ตามวันที่เลือก
- หน้ารายละเอียดห้องแสดงเวลาทั้งสองช่วง
- Staff Portal ตารางรายวันเปลี่ยน timeline ตามวันที่เลือก
- หน้าแก้ไขห้องระบุว่า field เวลาเดิมเป็นวันจันทร์ – ศุกร์ และเสาร์ – อาทิตย์ใช้ policy กลาง

ผลกระทบ:

- ไม่มี database migration และไม่เปลี่ยนข้อมูลห้องเดิม
- วันจันทร์ – ศุกร์ยังใช้ช่วงเวลาเดิมของแต่ละห้อง
- วันเสาร์ – อาทิตย์เปิดให้จองถึง `17:00 น.`
- request ที่ดัดแปลงจาก browser เพื่อจองนอกเวลาจะถูก backend ปฏิเสธ

แผนทดสอบ production:

1. เลือกวันจันทร์ – ศุกร์ ต้องเห็น slot ตามเวลาเดิม เช่น `08:30 – 16:30 น.`
2. เลือกวันเสาร์ – อาทิตย์ ต้องเห็น slot `09:00 – 17:00 น.` และจองช่วง `16:30 – 17:00 น.` ได้
3. ลองจองวันเสาร์ – อาทิตย์ก่อน `09:00 น.` ต้องถูกปฏิเสธ
4. ลองจองวันจันทร์ – ศุกร์หลัง `16:30 น.` ต้องถูกปฏิเสธ
5. เปิด Staff Portal ตารางรายวันของวันเสาร์ – อาทิตย์ ต้องเห็น timeline `09:00 – 17:00 น.`

การตรวจสอบก่อน push:

- `AST parse OK: service_hours.py, views.py, manage_views.py, forms.py`
- in-memory SQLite regression test ผ่าน:
  - วันจันทร์ – ศุกร์ `08:30 – 09:00 น.` สร้าง booking ได้
  - วันจันทร์ – ศุกร์ `16:30 – 17:00 น.` ถูก backend ปฏิเสธ
  - เสาร์ – อาทิตย์ `16:30 – 17:00 น.` สร้าง booking ได้
  - เสาร์ – อาทิตย์ `08:30 – 09:00 น.` ถูก backend ปฏิเสธ
- frontend weekday/weekend service-hours smoke check ผ่าน
- static integration check ผ่าน: backend validation, booking form และ Staff Portal timeline ใช้ policy กลาง
- `git diff --check` ผ่าน ไม่พบ whitespace error
- `python manage.py check` ยังรันไม่ได้ใน environment local เพราะไม่มี package `python-dotenv`
  แม้ระบุอยู่ใน `requirements.txt`

## Policy Update — Advance Booking 7 Service Days

<!-- Codex edit: low-impact booking policy update, production verified by system administrator. -->

สถานะ: ✅ ทดสอบผ่านบน production แล้ว ตามผลยืนยันจากผู้ดูแลระบบ

ปรับระยะเวลาจองล่วงหน้าสูงสุดเป็น `7` วันเปิดบริการ:

- ปรับ `MAX_ADVANCE_DAYS` ใน `booking/views.py` เป็น `7`
- เพิ่ม `max_advance_service_date()` ใน `booking/service_hours.py`
- backend ใช้ค่ากลางนี้ปฏิเสธ booking ที่เกินวันเปิดบริการลำดับที่ 7
- backend คำนวณวันสุดท้ายแล้วส่ง `max_booking_date` ให้ date picker โดยตรง
- อัปเดต `AGENTS.md`, `CLAUDE.md`, ต้นฉบับป้ายประชาสัมพันธ์ และคู่มือผู้ใช้

นิยามวันปิดที่ไม่นำมานับ:

- ใช้ `HolidayDate` ที่ active สำหรับวันที่สำนักวิทยบริการปิดทั้งวัน
- หากสำนักกำหนดปิดเสาร์หรืออาทิตย์ ให้เพิ่มวันดังกล่าวในรายการวันหยุด
- `RoomClosure` เป็นการปิดเฉพาะห้องหรือบางช่วงเวลา จึงไม่หักจากโควตา 7 วันของทั้งสำนัก

แผนทดสอบ production:

1. หากไม่มีวันปิด date picker ต้องเลือกวันเปิดบริการลำดับที่ `7` ได้
2. เพิ่มวันปิดทั้งสำนักคั่นกลาง แล้ว date picker ต้องเลื่อนวันสุดท้ายออกไปตามจำนวนวันปิด
3. สร้าง booking ในวันเปิดบริการลำดับที่ `7` ต้องสำเร็จ
4. request สร้าง booking หลังวันเปิดบริการลำดับที่ `7` ต้องถูก backend ปฏิเสธ

การตรวจสอบก่อน push:

- รอบก่อนปรับนิยาม: วันที่ `+7` สร้าง booking ได้ และวันที่ `+8` ถูก backend ปฏิเสธ
- service-day helper boundary test ผ่าน:
  - ไม่มีวันปิด: วันเปิดบริการลำดับที่ `7` ตรงกับวันที่ `+7`
  - มีวันปิดทั้งสำนักคั่นกลาง: วันสุดท้ายเลื่อนออกตามจำนวน `HolidayDate`
- in-memory SQLite integration test ผ่าน:
  - วันที่อยู่ใน `HolidayDate` ถูก backend ปฏิเสธ
  - วันเปิดบริการลำดับที่ `7` สร้าง booking ได้
  - วันถัดจากวันเปิดบริการลำดับที่ `7` ถูก backend ปฏิเสธ
  - หน้า booking ได้รับ `max_booking_date` ค่าเดียวกับ backend และรายการวันปิดที่เกี่ยวข้อง
- AST parse ผ่านสำหรับ `booking/service_hours.py` และ `booking/views.py`
- `git diff --check` ผ่าน ไม่พบ whitespace error
- `python -m py_compile` รันไม่สำเร็จ เพราะ environment local ไม่อนุญาตให้เขียนไฟล์ cache ใน `booking/__pycache__`
- `python manage.py check` ยังรันไม่สำเร็จใน environment local เพราะไม่มี package `python-dotenv`
  แม้ระบุอยู่ใน `requirements.txt`

ผลทดสอบ production จากผู้ดูแลระบบ:

- จองล่วงหน้าได้ครบ `7` วันเปิดบริการ
- ไม่นับวันปัจจุบันรวมในโควตา `7` วัน ตาม logic ที่กำหนด

## Repository Security Note

ระหว่างตรวจพบว่า Git remote URL เคยฝัง credential ไว้โดยตรง จึงลบ credential ออกจาก local remote config แล้ว
ควร revoke หรือ rotate token เดิมใน GitHub และใช้ Credential Manager หรือ GitHub CLI สำหรับการ push ครั้งถัดไป

## Git Delivery

- สร้าง docs-only commit: `c683769 docs: sync auth flow and poster service hours`
- push commit `c683769` ขึ้น `origin/master` สำเร็จแล้ว
- push Code Fix 1 commit: `5ff617b fix: stop registration when NPU account binding fails`
- push Code Fix 2 commit: `105fd6a fix: block inactive users from creating bookings`
- push docs verification commit: `46dfb64 docs: record inactive booking production verification`
- push Code Fix 3 commit: `a3377de fix: isolate LIFF profile cache by LINE user`
- push docs verification commit: `faf8579 docs: record LIFF cache production verification`
- push Code Fix 4 commit: `af77312 fix: enforce inactive user access across APIs`
- push docs verification commit: `dcb72d5 docs: record inactive API production verification`
- push Code Fix 5 commit: `c41e00f fix: enforce weekday and weekend booking hours`
- push policy update commit: `215d5b0 fix: allow bookings up to seven days ahead`
- push policy correction commit: `ba7460e fix: count advance bookings by service days`
- ไฟล์งานเดิมอื่นใน worktree ไม่ถูก stage หรือแก้ไขเพิ่มเติม

## Poster Planning — Publicity Sign Set

<!-- Codex edit: บันทึกจุดเริ่มต้นงานสื่อประชาสัมพันธ์หลังผ่าน production verification -->

เริ่มวางแผนชุดป้ายประชาสัมพันธ์แยกจากงานแก้ระบบ โดยกำหนดต้นฉบับ `8` แบบ:

- ป้ายเปิดตัวระบบ `1` แบบ
- ป้ายวิธีจอง `1` แบบ
- ป้ายกติกาสรุป `1` แบบ
- ป้ายหน้าห้องพร้อม QR Code จองตรง `5` แบบ

สร้างเอกสารกลาง `doc/poster-plan.md` สำหรับคุยงานทีละป้าย พร้อม checklist วัตถุดิบจริงของป้ายแรก:

- โลโก้หน่วยงาน
- QR Code จริงและลิงก์ปลายทางสำหรับทดสอบ
- ภาพบรรณารักษ์จริงที่ยินยอมให้ใช้เป็น reference
- ภาพสถานที่จริงของสำนักวิทยบริการ
- ขนาดงานพิมพ์และจุดติดตั้ง

แนวทางภาพ: ใช้บรรณารักษ์และบริบทห้องสมุดจริงเป็น reference แล้วแปลงเป็นภาพการ์ตูนน่ารัก
โดยแยกภาพประกอบออกจากข้อความและ QR Code เพื่อให้แก้ไขงานและทดสอบการสแกนได้แม่นยำ

## การตรวจสอบ

- ตรวจ implementation แบบ static review จาก `booking/views.py`, `booking/urls.py`, template LIFF และ `reserv/settings.py`
- ตรวจให้ `AGENTS.md` และ `CLAUDE.md` มีข้อมูลเชิงระบบชุดเดียวกัน
- บันทึกผลทดสอบ production เรื่อง auto-cancel และ IoT auto-off ตามข้อมูลยืนยันจากผู้ดูแลระบบ
- ตรวจ `git diff --check` สำหรับเอกสารที่แก้ไขแล้ว ไม่พบ whitespace error
- ไม่สามารถเปิด preview `doc/poster-booking-guide.html` ผ่าน in-app browser ได้ เนื่องจาก browser policy ไม่อนุญาต `file://` URL จึงตรวจข้อความจาก source HTML แทน
- ยังไม่ได้รัน Django check หรือ automated tests เพราะรอบนี้แก้เฉพาะเอกสาร
